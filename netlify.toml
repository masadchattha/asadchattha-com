# Netlify build config. `publish` is resolved relative to `base`,
# so this maps to main/dist in the repo.
[build]
  base = "main"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
